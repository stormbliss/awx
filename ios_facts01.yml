---
- name: Facts
  hosts: iosv
  gather_facts: false
  collections:
    - cisco.ios
  tasks:
  
    - name: Gather Facts - 01
      ios_facts:
       gather_subset: all
      register: all_subset
     
    - name: PRINT ALL SUBSETS GATHERED
      debug:
        var: all_subset
