---
  - name: Cisco IOS Command Demo
    hosts: csr1000v

    tasks:
      - name: Execute show commands
        ios_command:
          commands:
            - show run
            - show ip int brief
        register: sh_outputs
      - debug:
          msg: "{{sh_outputs.stdout_lines}}"

    
